# DjangoApp

This repository contains multiple independent Django projects created for learning.  
Each project has its own functionality and can be run separately.

## Features

- Basic Django setup with models, views, and templates  
- URL routing  
- Simple CRUD operations (Create, Read, Update, Delete)  
- Uses Django’s built-in admin panel  
